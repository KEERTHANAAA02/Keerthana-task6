# Keerthana-task6
The code implements K-Nearest Neighbors (KNN) classification using Scikit-learn. It includes data normalization, training/testing split, accuracy evaluation, and confusion matrix analysis. It also experiments with various K values and visualizes decision boundaries using PCA for 2D projection.
